---
title: "Work Doc"
author: "Palma and Wagner"
date: "11/16/2021"
output: html_document
---

```{r}
install.packages("nflfastR")
```

```{r}
library(nflfastR)
library(dplyr)
library(tidyverse)
library(ggplot2)
library(ggrepel)
pbp_2021 <- load_pbp(2021)
```

```{r}
pbp_2021 %>% 
  filter(half_seconds_remaining<300) %>% 
  group_by(game_id, drive) %>% 
  summarise( drive_play_count, as.integer(drive_game_clock_start), ydsnet)
  
```

